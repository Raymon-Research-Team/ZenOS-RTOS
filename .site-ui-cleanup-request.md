# Site UI cleanup

Requested changes:

- Hide the Releases button on the mobile layout of `docs/guide-html/index.html` only; keep desktop behavior unchanged.
- Remove the initial loading overlay and loader-only CSS/JS from `docs/guide-html/guide.html`.
- Preserve all Guide content, navigation, badges, buttons, and logos.
- Do not modify ZenOS core/source files.
- Do not delete existing project files.
